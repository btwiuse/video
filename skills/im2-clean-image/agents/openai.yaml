interface:
  display_name: "IM2 Material Clean Image"
  short_description: "Adds material, clean rendering, and targeted negative hygiene to IM2 prompts."
  default_prompt: "Use $im2-clean-image to rewrite this IM2 image prompt with material-light realism, clean anti-artifact controls, and a targeted avoid block."
policy:
  allow_implicit_invocation: true
